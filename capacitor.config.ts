import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.amarmuktagacha.cc',
  appName: 'Cc',
  webDir: '.vercel/output/static'
};

export default config;
